names= x
precondition=(x>=0) && (x<=50)
beforeloop=int loop_times = 10;
loopcondition=loop_times-- > 0
loop= if (x>50) x++;
		if (x < 0) x--;
		if (x == 50) {
			x --;
		} else if (x == 0) {
			x ++;
		} else {
			x += /*rand() % 3*/ - 1;
		}
postcondition=(x>=0) && (x<=50)
